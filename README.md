# Docker Node App Template

A simple, production-ready 📦 Docker template for a single Node.js application.

## Purpose
- Standardize Docker usage across projects
- Separate environment from code
- Serve as a base template for companies & teams

## Stack
- Node.js 20 (Alpine)
- Docker

## Build Image
```bash
docker build -t node-app .
